
	//Euclid's Algorithm

int gcd(int a,int b){
    //special case
    if(a < b){
        return gcd(b,a);
    }
    //base case
    if(b == 0){
        return a;
    }
    //Answer step applying euclid's algorithm
    return gcd(b,a%b);
}